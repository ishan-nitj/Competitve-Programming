#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define sl(n) scanf("%lld",&n)
#define pl(n) printf("%lld",n)
#define enter printf("\n")
#define space printf(" ")
#define MOD 1000000007
#define mag 100
#define rep(i,n) for(i=0;i<n;i++)
#define loop(i,a,b) for(i=(a);i<=(b);i++)
#define mset(x,v) memset(x, v, sizeof(x))
#define size 10
int main(){
cout.fill('0');// fill has no meaning w/o width.
cout.width(2);
cout<<3<<endl<<4<<endl;

cout.width(2);// fill() stays in effect till we change it but after printing one item width revert back to default
cout<<5;
}

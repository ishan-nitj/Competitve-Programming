#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define sl(n) scanf("%lld",&n)
#define pl(n) printf("%lld",n)
#define enter printf("\n")
#define space printf(" ")
#define MOD 1000000007
#define mag 100
#define rep(i,n) for(i=0;i<n;i++)
#define loop(i,a,b) for(i=(a);i<=(b);i++)
#define mset(x,v) memset(x, v, sizeof(x))
#define size 10

int main(){
cout.width(5);
cout<<44<<endl;

cout<<38<<endl;// after printing one item width revert back to default// fill and precision remains till the end of the program.

cout.width(5);//width =5  not valid for 56
cout<<34<<56<<endl;

cout.width(2);// width smaller then no truncation
cout<<3444;
}
